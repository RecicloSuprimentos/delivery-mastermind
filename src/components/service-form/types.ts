export interface ServiceFormValues {
  code: string;
  type: string;
  customer: string;
  address: string;
  addressComplement?: string;
  email?: string;
  phone: string;
  timeWindow?: string;
  notes?: string;
}